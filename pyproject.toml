[project]
name = "postprophet"
version = "0.1.0"
description = "Prediction harness for social media reach — predicts probability of hitting 2x median impressions"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "openai>=1.0",
]
