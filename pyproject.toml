[project]
name = "postprophet"
version = "0.1.0"
description = "Prediction engine for social media reach"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "openai>=1.0",
]
